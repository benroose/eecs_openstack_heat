heat_template_version: 2013-05-23

description: Template for security groups used in stack.

parameters:

resources:
  security_group:
      type: OS::Neutron::SecurityGroup
      properties:
        name: ping_security_group
        rules:
          - protocol: icmp


outputs:
